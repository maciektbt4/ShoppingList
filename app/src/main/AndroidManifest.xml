<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shoppinglist">
    <permission
        android:name="com.example.shoppinglist.CUSTOM_PERMISSION"
        android:protectionLevel="signature" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList"
        tools:targetApi="31">
        <activity
            android:name="com.example.shoppinglist.logging.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".settings.AppSettings"/>
        <activity android:name=".ui.shoppinglist.ShoppingActivity"/>
        <activity android:name=".ui.shoppinglist.EditShoppingItem"
            android:exported="true"/>
        <activity android:name=".logging.RegisterActivity"/>
    </application>

</manifest>